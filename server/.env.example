PORT=5000
NODE_ENV=development

# sqlite (default, zero-setup) or mysql (as specified in the proposal)
DB_DRIVER=sqlite

# MySQL settings (used when DB_DRIVER=mysql)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=kstu_counseling

JWT_SECRET=change_this_to_a_long_random_secret
CLIENT_URL=http://localhost:5173
PEER_PORT=9000
