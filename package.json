{
  "name": "kstu-counseling-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Smart Web-Based Counseling Platform for Client Tracking and Online Counseling Access at KSTU",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --include=dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "seed": "npm run seed --prefix server",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix server",
    "build:deploy": "npm run install:all && npm run build"
  },
  "engines": {
    "node": ">=18"
  },
  "author": "Cleopas Kwame Obbo",
  "license": "MIT",
  "dependencies": {
    "docx": "^9.7.1"
  }
}
